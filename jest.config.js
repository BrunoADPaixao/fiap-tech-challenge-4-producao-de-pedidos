module.exports = {
  testEnvironment: "node",
  coveragePathIgnorePatterns: ["/node_modules/"],
  // Configura o setup para os testes, se necessário
  // setupFilesAfterEnv: ['./tests/setup.js'],
};
